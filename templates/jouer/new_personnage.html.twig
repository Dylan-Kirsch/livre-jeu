{% extends 'base.html.twig' %}

{% block title %}Salutation{% endblock %}

{% block body %}

<div class="row">
    <div class="bg-white p-3 rounded-1">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        
        {{ form_end(form) }}
    </div>
    <div class="d-flex flex-row-reverse rounded-1">
        <a href="#"><button class="bg-danger fs-3 text-light p-2">Créer</button></a>
        <a href="#"><button class="bg-danger fs-3 text-light p-2">Retour</button></a>
    </div>
</div>
{% endblock %}